# Pinned versions — all have prebuilt wheels for Windows/macOS/Linux (Python 3.10-3.14),
# so no C compiler is ever needed. Plain uvicorn (not [standard]): pure-Python,
# avoids compiled extras that break installs on new Python versions.
fastapi==0.136.3
uvicorn==0.48.0
httpx==0.28.1
python-dotenv==1.2.2
